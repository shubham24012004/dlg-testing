repos:
  - repo: local
    hooks:
      - id: pytest
        name: Run backend test suite (pytest)
        entry: D:/DLG/dlg-analysis/venv/Scripts/python.exe -m pytest
        language: system
        pass_filenames: false
        always_run: true
